import React from 'react';
import ReactDOM from 'react-dom/client';
import BridgeBelote from '../application/BridgeBelote';

const root      = ReactDOM.createRoot( document.getElementById( 'ReactApplication' ) );
root.render(
    <BridgeBelote />
);
